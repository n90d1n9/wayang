/**
 * Target package for session decoding, configuration, action policy defaults,
 * configurable source registries, registry composition helpers, source
 * capability metadata, source diagnostics, source policy, source-spec
 * factories, source-spec redaction, load attempts, lookup and object-storage
 * adapters, request-context resolution, state continuity, profiles, and session
 * results.
 */
package tech.kayys.wayang.a2ui.wayang.session;
