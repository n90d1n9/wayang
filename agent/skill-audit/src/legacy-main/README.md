# Legacy Skill Audit

This source tree is intentionally not compiled by Maven.

The preserved implementation targeted the old `tech.kayys.gollek` package tree
and bundled audit, validation, analytics, versioning, and RBAC concerns against
stale repository contracts. The active module now keeps audit/analytics
utilities independent from lifecycle management and current Wayang SPI modules.

Keep this directory only as migration reference material.
