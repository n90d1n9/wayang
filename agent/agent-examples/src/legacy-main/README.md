# Legacy Agent Examples

This source tree is intentionally not compiled by Maven.

The preserved examples targeted old `Agent`, `AgentContext`, `AgentResponse`,
and multi-agent coordination APIs that are no longer part of the active agent
surface. Active examples now demonstrate the current `AgentClient`,
`InferenceBackend`, and `AgentSkill` contracts.

Keep this directory only as migration reference material.
